import React from 'react';

import { FlexContainer, Label } from '../../components';

export const Home = () => {
  return (
    <FlexContainer>
      <Label>home works</Label>
    </FlexContainer>
  );
};
